name: ark-shop-nestjs

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nestjs-app
    restart: unless-stopped
    user: "root"
    ports:
      - '${PORT:-3000}:3000'
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - .:/app
      - /app/node_modules
    command: >
      sh -c "
      npm install &&
      npx prisma generate &&
      npm run start:dev
      "
    networks:
      - nestjs-network

  # postgres:
  #   image: postgres:15-alpine
  #   container_name: nestjs-postgres
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   ports:
  #     - '5432:5432'
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-ark-shop_db}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 10s
  #   networks:
  #     - nestjs-network

networks:
  nestjs-network:
    driver: bridge
